# Audio

Place a locally hosted audio file here (e.g. `song.mp3`) and set `music.src`
in `src/content/siteContent.ts` to a path like `/audio/song.mp3` to enable the
optional background music control. It stays off by default and is fully
controlled by the visitor, leave `music.src` set to `null` to hide the
control entirely.
